# Memory Rules

1. In this project, whenever any change is made, it must be committed with a valid, meaningful commit message and pushed to `origin/main` immediately.
2. The `venv/` directory must never be pushed to the repository (covered in `.gitignore`).
3. Always respond in English for this project, but keep the words and sentences simple and easy to understand (avoid hard or technical words where a simple one works).
